import { Analytics } from "@vercel/analytics/react";

export default function ConduitDocsApp({ Component, pageProps }) {
  return (
    <>
      <Component {...pageProps} />
      <Analytics />
    </>
  );
}
